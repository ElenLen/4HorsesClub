# Лендинг "Клуб четырёх коней"

Адаптивный лендинг для международного шахматного турнира, созданный по принципу Pixel Perfect на основе [макета из Figma](https://disk.yandex.ru/d/xYAclHtuJVlChg).

## 📋 Техническое задание

### Используемый стек

- **HTML5** — семантическая разметка
- **CSS3** — стилизация, адаптивная верстка
- **Чистый JavaScript** — без библиотек и фреймворков

### Ключевые требования

- **Pixel Perfect** — точное соответствие макету Figma
- **Адаптивность** — корректное отображение на всех разрешениях экрана (отсутствие горизонтального скролла, адаптация текста и изображений)
- **Оптимизация контента** — избегание дублирования текста в мобильной и десктопной версиях
- **Интерактивность**:
  - Работающая бегущая строка
  - Кнопки-якоря для навигации по странице
  - Две карусели с разной логикой работы
  - Дополнительные анимации

---

## 🎯 Функциональность

### Структура страницы

1.  **Шапка** (`<header>`) — логотип и название клуба.
2.  **Главный экран** (`<main>`) — ключевой заголовок, текст и две якорные кнопки "Поддержать" и "Подробнее".
3.  **Бегущая строка** — анимированный блок с цитатами.
4.  **Промо-секция** (`#support`) — информация о лекции и турнире с адаптивным макетом.
5.  **Этапы преображения** — интерактивная сетка/карусель, показывающая этапы развития Васюков.
6.  **Участники турнира** (`#participants`) — карусель с карточками шахматистов.
7.  **Футер** (`<footer>`) — информационная заметка.

### Интерактивные элементы

#### 1. Бегущая строка

- Реализована через CSS-анимацию (`@keyframes`, `transform: translateX`).
- Бесконечный, плавный скроллинг текста.

#### 2. Карусель участников

- **Тип:** Зацикленная (бесконечная).
- **Автопрокрутка:** Смена слайда каждые 4 секунды.
- **Управление:** Кнопками "Вперед/Назад".
- **Адаптивность:** Показывает 3 карточки на десктопе, 1 — на мобильных устройствах.
- **Счетчик:** Отображает текущую позицию (например, "3/6").

#### 3. Карусель этапов

- **Тип:** Не зацикленная.
- **Автопрокрутка:** Отсутствует.
- **Управление:** Только через кнопки навигации.
- **Индикаторы:** Точечная навигация, показывающая текущий слайд.
- **Адаптивность:** На десктопе отображается как CSS Grid, на мобильных (≤ 380px) преобразуется в карусель.

#### 4. Навигация

- Кнопки **"Поддержать шахматную мысль"** и **"Подробнее о турнире"** на первом экране являются якорными ссылками и ведут к соответствующим секциям (`#support` и `#participants`).

---

## 🛠 Структура проекта

```
project/
├── fonts/                    # Шрифты
├── images/                   # Графические ресурсы (логотипы, изображения)
├── js/
│   └── script.js             # Вся логика на чистом JavaScript
├── styles/
│   ├── adaptive.css          # Адаптивные стили (медиа-запросы)
│   └── style.css             # Основные стили
├── index.html                # Основной
└── README.md                 # описание проекта
```

---

## 🔧 Логика JavaScript (`script.js`)

### Основные модули

#### Карусель участников

```javascript
// Базовая настройка
let visible = 3; // Количество видимых карточек
if (width <= 380) {
  visible = 1;
} // Адаптация под мобильные

// Автоматическая прокрутка
setInterval(nextSlide, 4000);

// Обновление позиции карусели и счетчика
function updateCarousel() {
  const shift = (100 / visible) * index;
  carousel.style.transform = `translateX(-${shift}%)`;
  counterText.textContent = `${Math.min(index + visible, total)}`;
}
```

#### Карусель этапов (активна только на мобильных)

```javascript
// Логика включается только при ширине экрана ≤ 380px
if (width <= 380) {
  // Показ активного этапа и обновление индикаторов
  function showItem(indx) {
    const targetClass = `cell-${indx + 1}`;
    slideItems.forEach((block) => {
      block.classList.toggle("active", block.classList.contains(targetClass));
    });
    slideCircle.forEach((item, i) => {
      item.classList.toggle("active", i === indx);
    });
  }
}
```

---

## 🎨 Особенности верстки

### Pixel Perfect

- Точное соответствие отступов, размеров и типографики макету Figma.
- Внимание к деталям: межстрочные интервалы, выравнивание, отступы.

### Семантичность и доступность

- Использование тегов `<section>`, `<header>`, `<footer>`, `<nav>`, `<figure>`/`<figcaption>`.
- ARIA-атрибуты для элементов управления (`aria-label`).
- Альтернативные тексты для всех изображений (`alt`).
- Корректная иерархия заголовков (`<h1>` — `<h3>`).

### Техники верстки

- **CSS Grid** — для сложных макетов (этапы преображения на десктопе).
- **Flexbox** — для выравнивания и простых сеток.
- **CSS Переменные (Custom Properties)** — для централизованного управления цветами и отступами.
- **Медиа-запросы (`@media`)** — для реализации адаптивности.

---

## 🚀 Запуск проекта

Проект не требует сборки или установки зависимостей.

1.  Склонируйте репозиторий или скачайте файлы.
2.  Откройте файл `index.html` в любом современном браузере (Chrome, Firefox, Edge, Safari).
3.  Для удобства разработки рекомендуется использовать расширение Live Server для VS Code.

---

## 📝 Примечания

- Проект написан на **чистом JavaScript** без использования сторонних библиотек (jQuery, React и т.д.) или фреймворков.
- Поддержка **IE11 не гарантируется**. Фокус на современных стандартах.
- Весь визуальный контент (персонажи, цитаты) является вымышленным и отсылает к роману Ильфа и Петрова "Двенадцать стульев".
